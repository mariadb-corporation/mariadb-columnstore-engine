select distinct l_linenumber from lineitem order by 1;
select distinct l_partkey from lineitem order by 1;
select distinct l_suppkey from lineitem order by 1;
select distinct l_quantity from lineitem order by 1;
select distinct l_discount from lineitem order by 1;
select distinct l_tax from lineitem order by 1;
select distinct l_returnflag from lineitem order by 1;
select distinct l_linestatus from lineitem order by 1;
select distinct l_shipinstruct from lineitem order by 1;
select distinct l_shipmode from lineitem order by 1;

select distinct l_partkey, l_suppkey from lineitem where l_orderkey <= 1000 order by 1, 2;
select distinct l_returnflag, l_linestatus from lineitem order by 1, 2;
select distinct l_shipmode, l_shipinstruct from lineitem order by 1, 2;
select distinct l_discount, l_tax from lineitem order by 1, 2;

select distinct l_partkey, l_suppkey from lineitem where l_orderkey < 10000 order by 1, 2;
select distinct l_returnflag, l_linestatus from lineitem where l_orderkey < 10000 order by 1, 2;
select distinct l_shipmode, l_shipinstruct from lineitem where l_orderkey < 10000 order by 1, 2;
select distinct l_discount, l_tax from lineitem where l_orderkey < 10000 order by 1, 2;

